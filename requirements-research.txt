# research/ 子包的额外依赖：研报复现（FFScore、CSCV、风险预算）所需的重依赖。
# 核心栈（回测引擎、策略族、akshare 数据层、ETF/期货示例）只需
# 仓库根 requirements.txt；只有跑 research/ 下的研报复现才需要本文件。
# 安装：pip install -r requirements-research.txt

pandas>=1.5
numpy>=1.21
scipy>=1.10
akshare>=1.15
pyarrow>=12.0
scikit-learn>=1.2
lightgbm>=4.0
cvxpy>=1.3
matplotlib>=3.5
seaborn>=0.12
tqdm>=4.60
pytest>=7.0
